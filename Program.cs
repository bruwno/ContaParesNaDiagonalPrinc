/* @@@ Questão 1 de 2 (2 pontos): Faça um procedimento que receba uma matriz bidimensional 
           de números inteiros. Se o tamanho da primeira dimensão da matriz for igual ao tamanho 
           da segunda dimensão, o programa deve imprimir os elementos pares que se encontram na 
           diagonal principal da matriz. Caso o tamanho da primeira dimensão da matriz seja 
           diferente do tamanho da segunda dimensão, o programa deve imprimir "Não é possível 
           percorrer a diagonal principal.

           -----
           Matriz quadrada: possui o mesmo número de linhas e colunas.

           Na matemática:
           Diagonal primária (principal): i = j
           Diagonal secundária          : i + j = n +1
*/

using System;

namespace ContaParesNaDiagonalPrinc
{
    public class Program
    {
        public static void Main()
        {
            Menu.ShowMenu();
        }

    }
}